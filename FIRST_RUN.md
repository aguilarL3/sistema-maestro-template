# 👋 Primera vez acá

Este vault todavía no está personalizado. La inicialización es **interactiva y desde adentro de esta carpeta** — no se personaliza mecánicamente desde afuera. Dos caminos:

**Con agente de IA (recomendado):** abrí Claude Code (u otro agente) **en esta carpeta** y decile **"Run onboarding"** (o `/onboarding`). Te entrevista, personaliza el vault y borra este archivo.

**A mano (deliberado):** (1) copiá `owner.env.example` a `owner.env`, completalo y corré `FORCE=1 ./personalize.sh` (el `FORCE=1` saltea a propósito el guard que evita la inicialización desde afuera); (2) llená los stubs de `01 Index/` (Vision, Objetivos, Mapa Personal); (3) borrá este archivo.

Después: leé [00 Inicio Rapido](<00 Inicio Rapido.md>).
